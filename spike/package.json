{
  "name": "spike",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Phase 0 throwaway spike — proves OpenRouter structured triage + tool-approval persist/resume against the live API. Standalone: own bun install, kept OUT of the root workspace so the main lock stays clean.",
  "scripts": {
    "triage": "bun run src/triage.ts",
    "pause": "bun run src/approval-pause.ts",
    "resume": "bun run src/approval-resume.ts"
  },
  "dependencies": {
    "@effect/ai-openrouter": "4.0.0-beta.93",
    "effect": "4.0.0-beta.93"
  },
  "devDependencies": {
    "@types/bun": "^1.3.14"
  }
}
