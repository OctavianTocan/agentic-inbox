/**
 * Font class names to apply to the root HTML element.
 *
 * Font families are loaded via @fontsource imports in globals.css.
 * CSS custom properties (--font-sans, --font-mono, --font-serif, --font-display)
 * are defined in globals.css and referenced by Tailwind / component styles.
 */
export const fonts = "touch-manipulation font-sans antialiased";
