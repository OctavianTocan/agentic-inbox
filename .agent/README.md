# Repo Agent State

Project-local skills live in `skills/`. They are copied into this repo so the
template can carry its frontend/design/workflow conventions without depending on
the original source checkout.
