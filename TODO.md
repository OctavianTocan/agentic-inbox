# TODO

## Deploy

# DONE

- [x] Make production deploy usable in showcase mode without an AI API key #deploy #vercel
  - Demo mode when `DATABASE_URL` or `OPENROUTER_API_KEY` is missing: seeded inbox + bottom-left sticker.
  - Both set → normal live path.
- [x] Repo hygiene, Contract tools, API fold into Next, Vercel config (PR #2)
