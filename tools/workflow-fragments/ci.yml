#<gen-github-workflow>
# name: ci
# required: true
# on:
#   push:
#     branches:
#       - main
#       - master
#   pull_request: {}
# permissions:
#   contents: read
# concurrency:
#   group: ci-${{ github.ref }}
#   cancel-in-progress: true
# jobs:
#   build:
#     name: CI
#     runs-on: ubuntu-latest
#     timeout-minutes: 15
#     env:
#       DATABASE_URL: postgres://agentic_inbox:agentic_inbox@localhost:5442/agentic_inbox
#       TEST_DATABASE_URL: postgres://agentic_inbox:agentic_inbox@localhost:5442/agentic_inbox_test
#     services:
#       postgres:
#         image: postgres:17-alpine
#         env:
#           POSTGRES_USER: agentic_inbox
#           POSTGRES_PASSWORD: agentic_inbox
#           POSTGRES_DB: agentic_inbox
#         ports:
#           - 5442:5432
#         options: >-
#           --health-cmd "pg_isready -U agentic_inbox -d agentic_inbox"
#           --health-interval 10s
#           --health-timeout 5s
#           --health-retries 5
#     steps:
#       - uses: actions/checkout@v5
#       - uses: oven-sh/setup-bun@v2
#       - uses: actions/cache@0057852bfaa89a56745cba8c7296529d2fc39830
#         with:
#           path: ~/.bun/install/cache
#           key: ${{ runner.os }}-bun-${{ hashFiles('bun.lock') }}
#           restore-keys: |
#             ${{ runner.os }}-bun-
#       - run: bun install --frozen-lockfile
#       - run: sudo apt-get update && sudo apt-get install -y postgresql-client
#       - run: |
#           PGPASSWORD=agentic_inbox psql -h localhost -p 5442 -U agentic_inbox -d agentic_inbox -v ON_ERROR_STOP=1 -c "SELECT 1 FROM pg_database WHERE datname = 'agentic_inbox_test'" | grep -q 1 \
#             || PGPASSWORD=agentic_inbox psql -h localhost -p 5442 -U agentic_inbox -d agentic_inbox -v ON_ERROR_STOP=1 -c "CREATE DATABASE agentic_inbox_test OWNER agentic_inbox;"
#       - run: bun run ci
#   sentrux:
#     name: Sentrux
#     runs-on: ubuntu-latest
#     timeout-minutes: 10
#     steps:
#       - uses: actions/checkout@v5
#       - uses: oven-sh/setup-bun@v2
#       - run: bun install --frozen-lockfile
#       - run: curl -fsSL https://raw.githubusercontent.com/sentrux/sentrux/main/install.sh | sh
#       - run: bun run arch:sentrux
#   react-doctor:
#     name: React Doctor
#     runs-on: ubuntu-latest
#     timeout-minutes: 10
#     steps:
#       - uses: actions/checkout@v5
#       - uses: oven-sh/setup-bun@v2
#       - run: bun install --frozen-lockfile
#       - run: bunx react-doctor@latest --no-telemetry --root apps/web
#</gen-github-workflow>
