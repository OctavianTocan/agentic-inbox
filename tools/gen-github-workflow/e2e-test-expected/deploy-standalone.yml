# AUTO-GENERATED by gen-github-workflow -- DO NOT EDIT
# Source: services/standalone/config.ts

name: deploy-standalone
on:
  push:
    branches:
      - main
    paths:
      - services/standalone/**
permissions:
  contents: read
jobs:
  deploy:
    name: Deploy Standalone
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@v5
      - name: Deploy
        run: echo "deploying services/standalone/config.ts"
  reusable:
    name: Reusable Call
    uses: ./.github/workflows/shared.yml
    with:
      param: value
